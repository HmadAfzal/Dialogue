

export interface MessageType {
    body: string,
    conversationId:string,
    createdAt:string,
    id:string,
    senderId:string,
    updatedAt:string
}


export interface MessageBody {
    message:string
}